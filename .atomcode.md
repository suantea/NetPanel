## NetPanel 项目规则

- Go 项目，后端 + 前端（Vue/React）
- 上游仓库：PIKACHUIM/NetPanel
- 本地 fork：suantea/NetPanel
- PR 必须用 rebase 而非 merge upstream（仓库要求 required_linear_history）
- 开发分支：w/next-dev（worktree 在 ~/.worktrees/netpanel-next）
- 构建：go build ./...、前端 npm run build
